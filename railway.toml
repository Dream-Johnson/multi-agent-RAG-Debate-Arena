[build]
builder = "nixpacks"
buildCommand = "curl -LsSf https://astral.sh/uv/install.sh | sh && cd backend && $HOME/.local/bin/uv sync"

[deploy]
startCommand = "cd backend && $HOME/.local/bin/uv run uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
